1)
#include <stdio.h>
#include <math.h>
int main()
{
	printf("Naidem rasstoyanie mezhdu dvumya tochkami s zadannymy koordinatamy (x1, y1) (x2, y2)");
	int x1;
	int y1;
	int x2;
	int y2;
	printf("\nVvedem x1:");
	scanf_s("%d", &x1);
	printf("\nVvedem y1:");
	scanf_s("%d", &y1);
	printf("\nVvedem x2:");
	scanf_s("%d", &x2);
	printf("\nVvedem y2:");
	scanf_s("%d", &y2);
	float rass;
	rass = sqrt((float)pow((x2 - x1), 2) + pow((y2 - y1), 2));
	printf("\nRasstoyanie mezhdu tochkami=%f", rass);
	return 0;
}
  
  
  2)
  #include <stdio.h>
#include <math.h>
int main()
{
	printf("Naidem dliny otrezkov iz tochek (a, b, c) na chislovoy osi i ix summu");
	int ac; int a; int b; int c;
	int bc;
	int sum;
	printf("\nVvedem a: ");
	scanf_s("%d", &a);
	printf("\nVvedem b: ");
	scanf_s("%d", &b);
	printf("\nVvedem c: ");
	scanf_s("%d", &c);
	ac = abs(a - c);
	bc = abs(b - c);
	sum = ac + bc;
	printf("(\nDlina AC=%i, \nDlina bc= %i, \nSumma dlin= %i)", ac, bc, sum);
	return 0;
}



3)
#include <stdio.h>
#include <math.h>
int main()
{
	printf("Naidem proizvedenie otrezkov iz tochek (a, b, c) na chislovoy osi pri chem c mezhdu a i b ");
	int ac; int a; int b; int c;
	int bc;
	int pr;
	printf("\nVvedem a: ");
	scanf_s("%d", &a);
	printf("\nVvedem b: ");
	scanf_s("%d", &b);
	printf("\nVvedem c: ");
	scanf_s("%d", &c);
	if (((a < c) && (c < b)) || ((a > c) && (c > b)))
	{
		ac = abs(a - c);
		bc = abs(b - c);
		pr = ac * bc;
		printf("\nProizvedenie dlin= %i", pr);
	}
	else
	{
		printf("\nNe sootvetstvuet usloviu zadachi");
	}
	return 0;
}


4)#include <stdio.h>
#include <math.h>
int main()
{
	printf("Naidem dloschad i perimetr pryamougolnika s vershinami (x1, y1) i (x2, y2) ");
	int x1; int x2; int y1; int y2; int plosch; int per; int a; int b;
	printf("\nVvedem x1: ");
	scanf_s("%d", &x1);
	printf("\nVvedem y1: ");
	scanf_s("%d", &y1);
	printf("\nVvedem x2: ");
	scanf_s("%d", &x2);
	printf("\nVvedem y2: ");
	scanf_s("%d", &y2);
	a = abs(y2 - y1);
	b = abs(x1 - x2);
	per = (a + b) * 2;
	plosch = a * b;
	printf("(\nPerimetr= %i, \nPloschad= %i)", per, plosch);
	return 0;
}


5)
#include <stdio.h>
#include <math.h>
int main()
{
	printf("Naidem perimetr i ploschad treugolnika s vershinami (x1, y1), (x2, y2), (x3, y3): ");
	float x1; float x2; float x3; float y1; float y2; float y3; float per; float plosch; float ab; float bc; float ca; float poluper;
	printf("\nVvedem x1: ");
	scanf_s("%f", &x1);
	printf("\nVvedem y1: ");
	scanf_s("%f", &y1);
	printf("\nVvedem x2: ");
	scanf_s("%f", &x2);
	printf("\nVvedem y2: ");
	scanf_s("%f", &y2);
	printf("\nVvedem x3: ");
	scanf_s("%f", &x3);
	printf("\nVvedem y3: ");
	scanf_s("%f", &y3);
	ab = sqrt(pow((x2 - x1), 2) + pow((y2 - y1), 2));
	bc = sqrt(pow((x3 - x2), 2) + pow((y3- y2), 2));
	ca = sqrt(pow((x3 - x1), 2) + pow((y3 - y1), 2));

	per = ab + bc + ca;
	poluper = per / 2;
	plosch = sqrt(poluper * (poluper - ab) * (poluper - bc) * (poluper - ca));
	printf("(\nPer= %f, \nPlosch= %f)", per, plosch);
	return 0;
}
