#include <stdio.h>
int main()
{
	printf("Naidem srednee arifmeticheskoe (sr) chisel a i b:");
	int a;
	int b;
	float sr;
	printf("\n Vvedite a:");
	scanf_s("%d", &a);
	printf("\n Vvedite b:");
	scanf_s("%d", &b);
	sr = ((float)a + b) / 2;
	printf("\n Srednee arifmeticheskoe = %f", sr);
	return 0;
}
