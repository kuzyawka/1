
1)
#include <stdio.h>
int main()
{
	printf("Pomenyaem znachenia peremennix a i b mestami:");
	int a; int b; int x;
	printf("\nVvedem a: ");
	scanf_s("%d", &a);
	printf("\nVvedem b: ");
	scanf_s("%d", &b);
	x = a;
	a = b;
	b = x;
	printf("(\nZnachenie a= %i, \nZnachenie b= %i", a, b);
	return 0;
}



2)
#include <stdio.h>
int main()
{
	printf("Pomenyaem znacheniya b na a,c na b, a na c: ");
	int a; int b; int c; int x;
	printf("\nVvedem a: ");
	scanf_s("%i", &a);
	printf("\nVvedem b: ");
	scanf_s("%i", &b);
	printf("\nVvedem c: ");
	scanf_s("%i", &c);
	x = a;
	a = c;
	c = b;
	b = x;
	printf("(\Znachenie a= %i, \nZnachenie b= %i, \nZnachenie c= %i)", a, b, c);
	return 0;
}



3)
#include <stdio.h>
int main()
{
	printf("Izmenim znachenie peremennoy c na a, c na b, a na c:");
	int a, b, c, x1, x2;
	printf("\nVvedem znachenie peremennoi a: ");
	scanf_s("%i", &a);
	printf("\nVvedem znachenie peremennoi b: ");
	scanf_s("%i", &b);
	printf("\nVvedem znachenie peremennoi c: ");
	scanf_s("%i", &c);
	x1 = b;
	b = c;
	c = a;
	a = x1;
	printf("(\nZnachenie a= %i, \nZnachenie b= %i, \nZnachenie c= %i)", a, b, c);
	return 0;
}
 
 
 
4)
#include <stdio.h>
#include <math.h>
int main()
{
	printf("Naidem znachenie funkzii pri dannom x: ");
	int x, rez;
	printf("\nVvedem x: ");
	scanf_s("%i", &x);
	rez = 3 * pow(x, 6) - 6 * pow(x, 2) - 7;
	printf("\nRezultat= %i", rez);
}



5)
#include <stdio.h>
#include <math.h>
int main()
{
	printf("Naidem znachenie funkzii pri dannom x: ");
	int x, rez;
	printf("\nVvedem x: ");
	scanf_s("%i", &x);
	rez = 4 * pow((x-3), 6) - 7 * pow((x-3), 3) +2;
	printf("\nRezultat= %i", rez);
}


6)
#include <stdio.h>
#include <math.h>
int main()
{
	printf("Naidem znachenie a^8 ispolzuya vspomogatelnuy peremennuy i 3 operazii umnozheniya: ");
	int a, x;
	printf("\nVvedem a: ");
	scanf_s("%i", &a);
	x = pow(a, 4) * pow(a, 2) * a * a;
	printf("\nZnachenie= %i", x);
	return 0;
}


7)
#include <stdio.h>
#include <math.h>
int main()
{
	printf("Naidem znachenie a^15 ispolzuya 2 vspomogatelnie peremennie i 5 operaziyami umnozheniya: ");
	int a, x, rez;
	printf("\nVvedem a: ");
	scanf_s("%i", &a);
	x = pow(a, 3);
	rez =pow(a, 2);
	printf("\rrezultat= %i", x*x*x*x*rez*a);
	return 0;
}
